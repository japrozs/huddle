import React from "react";
import { Text, View } from "react-native";

interface settingsProps {}

export const SettingsMainPage: React.FC<settingsProps> = ({}) => {
    return (
        <View>
            <Text>this is the settings.tsx</Text>
        </View>
    );
};
