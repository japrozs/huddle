mutation updateEvent(
    $desc: String!
    $tagLine: String!
    $name: String!
    $location: String!
    $eventId: Int!
) {
    updateEvent(
        eventId: $eventId
        name: $name
        tagLine: $tagLine
        desc: $desc
        location: $location
    )
}
